pipeline {
     agent any
     stages{
        stage('terraform init') {
            steps{
                sh "terraform inittets"
                sh "terraform apply -var-file=variables.tf -auto-approve"
            }
        }
    }
}
